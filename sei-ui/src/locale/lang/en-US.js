import setLang from '../lang';

const lang = {
    i: {
        locale: 'en-US',
        login: {
            account: '账号',
            password: '密码'
        }
    }
};

setLang(lang);

export default lang;
